# Assignment 2 - CampusCart API (CSCI 5709)

- **Assignment2_Report.docx** — the full written report (endpoint definitions,
  security endpoints, ERD, CRUD mapping). Still needs: live URLs, source code
  link, and Postman/Atlas screenshots pasted in — see the report itself and
  `backend/README.md` for exactly what to add and where.
- **Assignment2_ERD_Diagram.png** — the entity relationship diagram embedded in
  the report (also available standalone).
- **backend/** — the Node.js/Express/MongoDB API implementing the Product
  Listing feature (the 2 required endpoints + supporting auth), ready to deploy
  to Render. See `backend/README.md` for setup, local testing, and deployment
  steps.
- **report_assets/** — the scripts used to generate the report and ERD diagram
  (not part of the submission; kept for editing/regenerating if needed).
